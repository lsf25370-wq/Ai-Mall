-- ============================================================
-- 迁移 V3：电商闭环增强
-- 1. order 表新增 confirm_time（确认收货时间）
-- 2. 状态机扩展：5=退款中（买家申请后），卖家审批通过->6已退款，拒绝->回原状态
-- ============================================================

ALTER TABLE `order` ADD COLUMN `confirm_time` DATETIME DEFAULT NULL COMMENT '确认收货时间' AFTER `ship_time`;
